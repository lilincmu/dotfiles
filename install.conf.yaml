- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.vimrc:
    ~/.tmux.conf:
    ~/.gitconfig:

- shell:
  - [git submodule update --init --recursive, Installing submodules]

